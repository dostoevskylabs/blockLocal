{
  "manifest_version":2,
  "name": "blockLocal",
  "version": "1.0",
  "description": "proof of concept to block localhost resources, currently only supports fetch requests",
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["main.js"],
      "run_at": "document_start"
    }
  ]
}
